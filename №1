# Введення кількості елементів
n = int(input("Введіть кількість елементів масиву (n): "))

# Введення елементів масиву з клавіатури
print(f"Введіть {n} цілих чисел:")
arr = [int(input()) for _ in range(n)]

# Виведення початкового масиву
print("Введений масив:", arr)

# Знаходження мінімального елемента
min_value = min(arr)

# Виведення результату
print("Мінімальний елемент масиву:", min_value)
